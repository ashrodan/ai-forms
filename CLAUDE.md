# Must knows

- We are building a python package - keep things abstract and generic

- No hacky solutions or work around unless user says so for a particular need.



Ensure you we write tests and run them
- `uv run pytest` for all tests or run specific files if needed
- Before creating temporary test files, check existing test coverage in `tests/` directory
- Use existing test patterns and extend current test files rather than creating new ones



# Notes
- `uv add <package>` to add new packages
